programa(){

    entero vec[10];
    entero z := - 3 mod - 4 / -5 + -5;
    entero x;
	booleano y := true;
	
	y := (x + 2 >= 1) Y (x * 2 <= 7);

	avanzar(1 + x);
	mirarArriba();

	y := !y;
	x := x+4;
	
	avanzar(vec[x] - 3);
	mirarAbajo();
	
	y := x < 2;
	
	avanzar(x);
	mirarIzquierda();
	
	x := x / 4;
	
	avanzar(vec[x * 2 + 3/3]);
	mirarDerecha();	
}
